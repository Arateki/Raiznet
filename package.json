{
  "name": "raiznet",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "^2.6.0",
    "@bufbuild/buf": "^1.54.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
